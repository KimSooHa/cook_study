package com.study.cook.repository;

import com.study.cook.dto.MemberDto;
import com.study.cook.dto.MemberLoginIdSearchCondition;
import com.study.cook.dto.MemberPwdSearchCondition;
import org.springframework.data.domain.Page;
import org.springframework.data.domain.Pageable;

import java.util.List;

public interface MemberRepositoryCustom {
//    List<MemberTeamDto> search(MemberSearchCondition condition);
//Page<MemberTeamDto> searchPageSimple(MemberSearchCondition condition, Pageable pageable)

//    String searchLoginId(MemberLoginIdSearchCondition condition);
//    String searchPwd(MemberPwdSearchCondition condition);

//    Page<MemberDto> searchPageSimple(MemberSearchCondition condition, Pageable pageable);
//    Page<MemberDto> searchPageComplex(MemberSearchCondition condition, Pageable pageable);
}

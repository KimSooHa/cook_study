version: '3.7'

services:
#  cook-app:
#    image: your-cook-app-image  # 또는 build 설정
#    container_name: cook-app
#    depends_on:
#      - mariadb
#    networks:
#      - bridge_net
#    ports:
#      - "8080:8080"
#    volumes:
#      - ./your-local-app:/app
#
#  mariadb:
#    image: mariadb:10.5
#    container_name: mariadb
#    restart: unless-stopped
#    environment:
#      MYSQL_ROOT_PASSWORD: root1234
#      MYSQL_DATABASE: your_db
#      MYSQL_USER: exporter
#      MYSQL_PASSWORD: exporter1234
#    ports:
#      - "3306:3306"
#    volumes:
#      - my-db-data:/var/lib/mysql
#    networks:
#      - bridge_net

  prometheus:
    image: prom/prometheus
    container_name: prometheus
    volumes:
      - ./prometheus/prometheus.yml:/etc/prometheus/prometheus.yml
    ports:
      - "9090:9090"

  grafana:
    image: grafana/grafana
    container_name: grafana
    ports:
      - "3000:3000"
    volumes:
      - grafana-storage:/var/lib/grafana
    depends_on:
      - prometheus

#  mysql_exporter:
#    image: prom/mysqld-exporter
#    container_name: mysql_exporter
#    restart: unless-stopped
#    environment:
#      - DATA_SOURCE_NAME=exporter:exporter1234@(172.17.0.3:3306)/
#    ports:
#      - "9104:9104"
#    networks:
#      - bridge_net  # 기본 bridge 네트워크에 붙이기
#    volumes:
#      - ./config/.my.cnf:/root/.my.cnf:ro
#    depends_on:
#      - cook-app


  redis_exporter:
    image: oliver006/redis_exporter
    container_name: redis_exporter
    ports:
      - "9121:9121"
    networks:
      - monitoring

networks:
  monitoring:

#networks:
#  bridge_net:
#    driver: bridge

volumes:
  grafana-storage:
#  my-db-data:
#    external: true